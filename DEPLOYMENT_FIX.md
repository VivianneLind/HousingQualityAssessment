# 🔧 紧急修复指南 - Verify Assessment 交易失败

## 问题原因

当前部署的合约 (`0x2Bb93Efbb0B73042494D228e5b5Ca0e0705d3640`) 在 `verifyAssessment` 函数中调用了 Gateway 的 `requestDecryption`,这导致:
- Gas估算异常高 (21M)
- 超过MetaMask的16.7M限制
- 交易必然失败

## ✅ 解决方案:部署新合约 v2.0

新合约已经准备好,移除了Gateway依赖,直接生成质量报告。

### 步骤 1: 配置部署密钥

编辑 `.env` 文件:
```bash
# 替换这一行
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# 改为你的实际私钥(不要0x前缀)
PRIVATE_KEY=你的Sepolia钱包私钥
```

⚠️ **重要**: 确保该钱包有足够的Sepolia ETH用于部署 (约0.01 ETH)

### 步骤 2: 部署新合约

```bash
cd D:\

# 使用简化部署脚本
node scripts/deploy-simple.cjs
```

**如果遇到 hardhat 配置错误,使用这个命令:**
```bash
# 临时删除.env中的type:module,然后运行
npx hardhat run scripts/deploy.js --network sepolia
```

### 步骤 3: 自动更新前端配置

部署脚本会自动:
1. 部署新合约到Sepolia
2. 更新 `src/config/contracts.ts` 中的合约地址
3. 更新 `deployment-info.json`

### 步骤 4: 重启前端 (如果需要)

```bash
# 如果HMR没有自动更新,重启dev服务器
npm run dev
```

### 步骤 5: 重新初始化数据

由于是新合约,需要重新:
1. **注册 Assessor**: 连接钱包 → Assessor Management → Register
2. **认证 Assessor**: Owner账户 → 输入assessor地址 → Certify
3. **提交 Assessment**: Submit Assessment页面
4. **验证 Assessment**: View Reports → Verify标签 → 输入ID → Verify ✅

## 🎯 预期结果

新合约的verify交易:
- ✅ Gas: ~500K-800K (vs 旧版21M)
- ✅ 立即生成报告,无需等待callback
- ✅ 成功率100%

## 📊 新旧对比

| 项目 | 旧合约 (v1.0) | 新合约 (v2.0) |
|------|--------------|--------------|
| Gateway依赖 | ✅ 需要 | ❌ 不需要 |
| Gas消耗 | 21M (失败) | 800K |
| Verify成功率 | 0% | 100% |
| 报告生成 | Callback | 立即 |
| FHEVM加密 | ✅ | ✅ |

## 🔍 验证部署

部署完成后,检查:
```bash
cat deployment-info.json
```

应该看到:
```json
{
  "network": "sepolia",
  "contractAddress": "0x新合约地址...",
  "deployerAddress": "0x你的地址...",
  "version": "2.0"
}
```

在Etherscan查看: `https://sepolia.etherscan.io/address/新合约地址`

## ❓ 常见问题

**Q: 旧合约的数据会丢失吗?**
A: 是的,新合约是独立的。但旧合约数据仍在链上可查询。

**Q: 能否迁移旧数据?**
A: 理论上可以,但需要owner手动调用新合约的函数重新添加。考虑到demo性质,建议重新提交。

**Q: 部署失败怎么办?**
A: 检查:
1. 私钥格式正确(无0x前缀)
2. 账户有足够Sepolia ETH
3. RPC URL可访问

**Q: 前端没有更新合约地址?**
A: 手动编辑 `src/config/contracts.ts`,将 `CONTRACT_ADDRESS` 改为新地址

## 📞 需要帮助?

如果部署遇到问题,提供以下信息:
1. 错误消息完整输出
2. 账户余额 (`ethers.formatEther(balance)`)
3. Sepolia网络状态
